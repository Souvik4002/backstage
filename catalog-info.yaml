apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: examples
  description: Example local component with TechDocs
  annotations:
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  owner: user:guest
  lifecycle: experimental

